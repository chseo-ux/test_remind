name: Build Design Tokens

on:
  push:
    branches: [ main ]
    paths:
      - 'tokens/**'

jobs:
  build:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          clean: true  # ⭐ 추가: 체크아웃 시 클린
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Clean build directory  # ⭐ 새로운 단계
        run: rm -rf build/
      
      - name: Build tokens
        run: npm run build:tokens
      
      - name: Commit and push built files
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add build/
          git diff --quiet && git diff --staged --quiet || (git commit -m "🎨 Build design tokens" && git push)
